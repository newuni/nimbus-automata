services:
  automata-web:
    container_name: automata-web
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BASE_PATH: /automata/
    restart: unless-stopped
    ports:
      - "127.0.0.1:8111:80"
